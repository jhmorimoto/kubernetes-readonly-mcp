# Kubernetes MCP server

Use the official MCP Python SDK documentation when changing protocol integration:
https://py.sdk.modelcontextprotocol.io/

This server must remain read-only. Do not add tools or Kubernetes client calls that create, patch, replace, delete, execute, attach, port-forward, watch, proxy, or otherwise mutate cluster state.