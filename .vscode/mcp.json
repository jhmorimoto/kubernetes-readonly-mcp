{
  "servers": {
    "kubernetes-readonly": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "main.py"],
      "env": {
        "KUBECONFIG_DIRECTORY": "${env:HOME}/.kube/configs"
      }
    }
  }
}